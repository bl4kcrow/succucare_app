import 'package:flutter/material.dart';

class PlantScanView extends StatelessWidget {
  const PlantScanView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Plant Scan View')));
  }
}
